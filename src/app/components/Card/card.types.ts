export interface HeroCardType {
  img: string;
  title: string;
  path: string;
  buttonText: string;
}

export interface NasaResenjaType {
  img: string;
  title: string;
  description: string;
  styleClass?: string;
}
